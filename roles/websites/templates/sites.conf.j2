{% for domain, settings in websites.iteritems() %}
<VirtualHost *:{{ apache_port }}>
  ServerName {{ domain | replace('__hostname__', hostname) }}
  DocumentRoot /var/www/{{ domain | replace('__hostname__', hostname) }}/public_html

  <IfModule mpm_itk_module>
    AssignUserId {{ domain | replace('__hostname__', hostname) }} {{ domain | replace('__hostname__', hostname) }}
  </IfModule>
</VirtualHost>

{% endfor %}
